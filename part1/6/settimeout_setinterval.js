"use strict";
//it'll first step to create first step by step game (28.02.20)

//setTimeout(func without braces, time in mc, art1, arg2, etc.) - launch function one time
//setInterval(func without braces, time in mc, art1, arg2, etc.) - many times
//clearTimeout(name of timeout variable) - decline the timeout
//clearInterval(name);

function sayHi(name) {
  alert(`hi ${name}`);
}
// let firstFunLaunch = setTimeout(sayHi, 5000, "Andrew"); // works, I've cheched

// clearTimeout(firstFunLaunch); //works

// let firtInterval = setInterval(sayHi, 2000, "Andrew"); //works
//
// clearInterval(firtInterval);
