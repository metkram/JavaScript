"use strict";

let twoDigSum = (dig1, dig2) => dig1 + dig2;
// the same as
// function twoDigSum(dig1, dig2) {
//   return dig1 + dig2;
// }

let sayHi = () => alert("Hi"); //arrow function without arguments

let saySomething = (sentense) => {
  let fullSentence = "You said: " + sentense;
  return fullSentence;
}
